version: '3.8'
name: payload-blog

services:
  postgres:
    image: postgres:16-alpine
    container_name: payload-blog-postgres
    restart: unless-stopped
    environment:
      POSTGRES_USER: payload
      POSTGRES_PASSWORD: payload
      POSTGRES_DB: payload_blog
      PGDATA: /var/lib/postgresql/data/pgdata
    volumes:
      - postgres_data:/var/lib/postgresql/data
      - ./docker/postgres/init:/docker-entrypoint-initdb.d
    ports:
      - '5234:5432'
    healthcheck:
      test: ['CMD-SHELL', 'pg_isready -U payload']
      interval: 10s
      timeout: 5s
      retries: 5
    networks:
      - payload-blog-network

  # app:
  #   build:
  #     context: .
  #     dockerfile: Dockerfile
  #   container_name: payload-blog-app
  #   restart: unless-stopped
  #   environment:
  #     NODE_ENV: production
  #     DATABASE_URI: postgresql://payload:payload@postgres:5432/payload_blog
  #     PAYLOAD_SECRET: ${PAYLOAD_SECRET:-your-secret-key-change-in-production}
  #     CMS_SEED_ADMIN_EMAIL: ${CMS_SEED_ADMIN_EMAIL:-admin@example.com}
  #     CMS_SEED_ADMIN_PASSWORD: ${CMS_SEED_ADMIN_PASSWORD:-admin}
  #     PORT: 3000
  #   ports:
  #     - '3000:3000'
  #   depends_on:
  #     postgres:
  #       condition: service_healthy
  #   networks:
  #     - payload-blog-network

volumes:
  postgres_data:
    driver: local

networks:
  payload-blog-network:
    driver: bridge
